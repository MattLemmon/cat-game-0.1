
      __cat-game-0.1__


If you have bundler, the required gems can be installed by simply typing:

$ bundle install


Otherwise, you should install the gosu gem and the chingu gem:

$ gem install gosu
$ gem install chingu



Basic Cat Game:

$ ruby cat-game.rb


Advanced Cat Game:

$ ruby cpncat.rb

